# PRD — <feature name>

> This is the only steering input Ralph gets. Be specific. Vague PRD → vague pile
> of commits. Delete this quote block and the angle-bracket placeholders before running.

## Goal

<One or two sentences: what are we building and why. The outcome, not the steps.>

## Deliverables

A concrete, checkable list of what must exist when this is done. Ralph turns each
of these into one-or-more small tasks.

- [ ] <Deliverable 1 — e.g. "POST /sessions endpoint that creates a session and returns its id">
- [ ] <Deliverable 2>
- [ ] <Deliverable 3>

## Constraints

<Non-negotiables: tech/library choices, patterns to follow, files NOT to touch,
performance/security requirements, the quality bar (prototype vs production).>

- <Constraint 1>
- <Constraint 2>

## Out of scope

<Explicitly list what Ralph should NOT build, so it doesn't wander.>

- <Non-goal 1>

## Done means

<The definition of done for the whole PRD. How a reviewer confirms it's complete —
e.g. "all deliverables checked, `npm test` green, the new endpoint covered by a test".>
